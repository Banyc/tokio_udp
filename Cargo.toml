[package]
name = "tokio_udp"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1"
socket2 = "0.6"
tokio = { version = "1", features = ["net", "io-util", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }
